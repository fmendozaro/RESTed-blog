const postAPI = require('./postsAPI');

test('get all posts', () => {
    expect(1+2).toBe(3);
    // function callback(data) => {
    //     expect(data).toBe({"id": 1});
    // }
    // postAPI.get(1);
});